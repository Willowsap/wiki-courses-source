export interface Course {
  title: string;
  description: string;
}

export interface CourseStub {
  title: string;
  description: string;
}

export interface Topic {
  title: string;
  contents: string;
}
